{
  "name": "bloom-luxury-florist-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r assets dist/ && cp index.html dist/ && cp 404.html dist/ && cp favicon.svg dist/ 2>/dev/null || true"
  }
}
